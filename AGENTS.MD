AGENTS.MD


---

## **🤖 AGENTS.md** (Git Commands Helper)

```markdown
# Git Commands for AI Assistant

## Initial Setup
```bash
git init
git add .
git commit -m "Initial commit: full coding interview platform"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
